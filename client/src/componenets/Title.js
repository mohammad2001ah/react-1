// arrow function
// last>>>> function name(){}
// arrow>>>> const name=(params)=>(code)   بستخدمه لسهولة ال sunatx تبع
import React from "react";

const Title =({text})=> <h1>{text}</h1>

export default Title;
